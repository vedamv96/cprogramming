#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("Enter the two numbers a b:");
    scanf("%d %d",&a,&b);
    if(a==b)
    {
       printf("Two numbers are equal");
    }
    else
    {
        printf("Two numbers are not equal");
    }

}
