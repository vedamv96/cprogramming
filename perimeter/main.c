#include <stdio.h>
#include <stdlib.h>

int main()
{
     int l,b,a;
     printf("Enter the length and breadth l,b:");
     scanf("%d %d",&l,&b);
     a=l*b;
     printf("Area of rectangle:%d",a);


}
